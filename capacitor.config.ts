import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.app.deltastatepensionsbureau.helpdesk',
  appName: 'help-desk',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
